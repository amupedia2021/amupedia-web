name: Lint and Test

on:
  pull_request:
    branches: [master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: npm install
    - name: Lint Code
      run: npx eslint .
    - name: Run Tests
      run: npm test
